/**
 * 
 * @author Kiran Rambha
 * This is a enum class which stores the type of the variable in the string.
 */
public enum Type {
	
	/**
	 * The different types of variables in the input string.
	 */
	NUMBER, SYMBOL, STRING, INVALID;
} 
